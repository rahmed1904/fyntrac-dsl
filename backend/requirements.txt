# Core framework
fastapi>=0.115,<1.0
uvicorn>=0.34,<1.0
starlette>=0.45,<2.0
python-multipart>=0.0.21

# Database
motor>=3.7,<4.0
pymongo>=4.12,<5.0

# Data validation & settings
pydantic>=2.12,<3.0
python-dotenv>=1.2,<2.0

# Data processing
pandas>=2.3,<3.0
openpyxl>=3.1,<4.0

# Requirements-document ingestion (agent reads PDF / Word requirement docs)
pypdf>=6.0,<7.0
python-docx>=1.1,<2.0

# MCP server (optional) — drive the agent from a personal Claude client.
# Only needed to run backend/mcp_server.py; the web app doesn't import it.
mcp>=1.28,<2.0

# Security / encryption
cryptography>=46.0,<47.0

# AI providers
openai>=1.99,<2.0
anthropic>=0.94,<1.0
google-genai>=1.57,<2.0
